{
  "name": "smartcar-api",
  "version": "1.0.0",
  "description": "An implementation of a small subset of the Smartcar spec",
  "main": "app.js",
  "scripts": {
    "test": "mocha '**/*.spec.js'",
    "start": "node app.js | bunyan",
    "coverage": "istanbul cover _mocha --exit $(find . -name *.spec.js -not -path './node_modules/*')"
  },
  "author": "Steve Sharp",
  "license": "MIT",
  "dependencies": {
    "bunyan": "^1.8.12",
    "express": "^4.16.2"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-http": "^3.0.0",
    "istanbul": "^0.4.5",
    "mocha": "^5.0.1"
  }
}
