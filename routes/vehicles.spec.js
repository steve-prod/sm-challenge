var chai = require('chai');
var chaiHttp = require('chai-http');
var server = require('../app');
var should = chai.should();

chai.use(chaiHttp);

describe('Smartcar API', function() {
    it('should retrieve properly formatted vehicle information', function(done) {

    });
    it('should retrieve properly formatted security information', function(done) {

    });
    it('should retrieve properly formatted fuel information', function(done) {

    });
    it('should retrieve properly formatted battery information', function(done) {

    });
    it('should execute engine actions properly', function(done) {

    });
});
